/**
 * BestTime Widget
 * AI-powered time suggestions for optimal content publishing
 * 
 * Usage:
 *   import { initBestTime, loadSuggestions, attachBestTimeEvents } from './widgets/best-time';
 *   
 *   initBestTime(config, channel, monthKey);
 *   attachBestTimeEvents();
 */

export * from './render';
export * from './actions';