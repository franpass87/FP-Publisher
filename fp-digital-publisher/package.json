{
  "name": "fp-digital-publisher",
  "version": "0.1.0",
  "description": "Development tooling for the FP Digital Publisher plugin.",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "dev": "node tools/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
